---
export const prerender = false;
import ThemedPage from '../../layouts/themed-page.astro';
import { CareersContent } from '../../components/pages/CareersContent';
import { initCMS, getJobPostings, getSiteSettings, getEmployeeBenefits, getCompanyAwards, getPageCopy, getUIStrings } from '../../lib/webflow-cms';

// Initialize CMS with Cloudflare runtime context
initCMS(Astro.locals);

let jobs: any[] = [];
let settings: any = {};
let benefits: any[] = [];
let awards: any[] = [];
let pageCopy: any = null;
let uiStrings: Record<string, string> = {};
try {
  const [jobsResult, siteSettings, benefitsResult, awardsResult, copy, ui] = await Promise.all([
    getJobPostings(),
    getSiteSettings(),
    getEmployeeBenefits(),
    getCompanyAwards(),
    getPageCopy('careers'),
    getUIStrings(),
  ]);
  jobs = jobsResult.items;
  settings = siteSettings;
  benefits = benefitsResult.items;
  awards = awardsResult.items;
  pageCopy = copy;
  // Serialize Map → plain object so it crosses the React island boundary.
  uiStrings = Object.fromEntries(ui);
} catch (error) {
  console.error('Error fetching careers data:', error);
}
---

<ThemedPage title="Careers | Jewett Junction" theme="dark" currentSection="careers">
  <CareersContent client:load theme="dark" jobs={jobs} settings={settings} benefits={benefits} awards={awards} pageCopy={pageCopy} uiStrings={uiStrings} />
</ThemedPage>
