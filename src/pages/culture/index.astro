---
export const prerender = false;
import ThemedPage from '../../layouts/themed-page.astro';
import { CultureContent } from '../../components/pages/CultureContent';
import { initCMS, getCultureStories, getSiteSettings, getCoreValues } from '../../lib/webflow-cms';

// Initialize CMS with Cloudflare runtime context
initCMS(Astro.locals);

let stories: any[] = [];
let settings: any = {};
let coreValues: any[] = [];
try {
  const [storiesResult, siteSettings, coreValuesResult] = await Promise.all([
    getCultureStories(),
    getSiteSettings(),
    getCoreValues(),
  ]);
  stories = storiesResult.items;
  settings = siteSettings;
  coreValues = coreValuesResult.items;
} catch (error) {
  console.error('Error fetching culture data:', error);
}
---

<ThemedPage title="Culture | Jewett Junction" theme="dark" currentSection="culture">
  <CultureContent client:load theme="dark" stories={stories} settings={settings} coreValues={coreValues} />
</ThemedPage>
