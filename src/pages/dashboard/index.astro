---
// Permanent redirect — the dashboard now lives at the mount root
// (/jewett-junction). This route exists only for legacy bookmarks and any
// stale links that still reference /jewett-junction/dashboard.
export const prerender = false;
return Astro.redirect('/jewett-junction', 308);
---
